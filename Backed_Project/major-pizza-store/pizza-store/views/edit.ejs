<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Pizza</title>
</head>
<body>
    <h1>Edit Pizza: <%= pizza.name %></h1>
    <form action="/items/<%= pizza._id %>" method="POST">
        <input type="hidden" name="_method" value="PUT">  <!-- To simulate a PUT request -->
        
        <label for="name">Name:</label>
        <input type="text" name="name" id="name" value="<%= pizza.name %>" required><br>
        
        <label for="description">Description:</label>
        <textarea name="description" id="description" required><%= pizza.description %></textarea><br>
        
        <label for="price">Price:</label>
        <input type="number" name="price" id="price" value="<%= pizza.price %>" required><br>
        
        <label for="imageUrl">Image URL:</label>
        <input type="url" name="imageUrl" id="imageUrl" value="<%= pizza.imageUrl %>" required><br>
        
        <button type="submit">Update Pizza</button>
    </form>
    <br>
    <a href="/items">Back to Pizza List</a>
</body>
</html>
