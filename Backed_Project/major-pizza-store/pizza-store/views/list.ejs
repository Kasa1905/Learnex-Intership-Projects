<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pizza Store - List</title>
</head>
<body>
    <h1>Pizza List</h1>
    <a href="/items/new">Add a New Pizza</a>
    <ul>
        <% pizzas.forEach(pizza => { %>
            <li>
                <h2><%= pizza.name %></h2>
                <p><%= pizza.description %></p>
                <p>Price: $<%= pizza.price %></p>
                <img src="<%= pizza.imageUrl %>" alt="<%= pizza.name %>" style="width: 200px;">
                <a href="/items/edit/<%= pizza._id %>">Edit</a> |
                <a href="/items/delete/<%= pizza._id %>" onclick="return confirm('Are you sure?')">Delete</a>
            </li>
        <% }); %>
    </ul>
</body>
</html>
